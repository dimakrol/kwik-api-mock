PORT=3099
CDV_FAIL_UNKNOWN=false
MOCK_BASE_URL=http://localhost:3099
# Company UUID for webhook URL when payment.company_uuid is unset (also settable in mock UI Settings)
MOCK_DEFAULT_COMPANY_UUID=
# Default webhook when payment has no notify_url; {companyUuid} from payment or MOCK_DEFAULT_COMPANY_UUID
MOCK_DEFAULT_NOTIFY_URL=http://localhost:3005/v1/webhook/kwik/{companyUuid}
